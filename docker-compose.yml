services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: tlfinc
    ports:
      - "5432:5432"
    volumes:
      - tlfinc_pgdata:/var/lib/postgresql/data

volumes:
  tlfinc_pgdata:
